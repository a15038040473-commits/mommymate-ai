services:
  - type: web
    name: mommymate-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false  # 在 Render 控制台手动填写
